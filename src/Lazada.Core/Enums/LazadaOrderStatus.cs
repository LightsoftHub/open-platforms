namespace Light.Lazada.Enums
{
    public enum LazadaOrderStatus
    {
        unpaid,
        pending,
        packed,
        repacked,
        ready_to_ship_pending,
        ready_to_ship,
        shipped,
        delivered,
        canceled,
    }
}
